services:
  snowmaking-system:
    build: .
    container_name: snowmaking-system
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - DB_DIR=/app/data
    volumes:
      - ./data:/app/data
    restart: unless-stopped
